============
Installation
============

At the command line::

    $ easy_install ds-py-utils

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv ds-py-utils
    $ pip install ds-py-utils